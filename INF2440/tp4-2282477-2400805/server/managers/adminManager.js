class AdminManager {
    constructor(fsManager) {
        this.fsManager = fsManager;
    }

    /**
     * TODO : Réinitialiser toutes les images : rendre publiques, réinitialiser le nombre de vues, désactiver la suppression immédiate
     */
    async resetImages() {
       
    }
}

module.exports = AdminManager;
